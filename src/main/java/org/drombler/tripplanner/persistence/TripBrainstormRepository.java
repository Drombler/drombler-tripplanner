package org.drombler.tripplanner.persistence;

import org.springframework.data.jpa.repository.JpaRepository;

public interface TripBrainstormRepository extends JpaRepository<TripBrainstormEntity, Long> {
}
